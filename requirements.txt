--extra-index-url https://download.pytorch.org/whl/cpu

# Núcleo web (se mantiene Django 4.2.16 del autor)
Django==4.2.16
# settings.py usa rest_framework y corsheaders, pero no estaban instalados
djangorestframework==3.15.2
django-cors-headers==4.3.1

# Configuración por entorno (se usa en settings.py y ya venía instalado)
python-dotenv==1.2.3

# Cliente del LLM: Ollama corre en el host (100.78.144.4, RTX 2060).
# La VM solo es cliente HTTP, no necesita GPU ni modelos locales.
langchain-core==0.2.41
langchain-ollama==0.1.3

# RAG: embeddings locales (CPU) + base vectorial persistente.
# Torch solo-CPU: la VM no tiene GPU; la variante +cpu pesa ~200 MB
# en vez de ~2.5 GB del paquete con CUDA.
sentence-transformers==3.1.1
torch==2.4.1+cpu
chromadb==0.5.5

# Usado por session_handler.py
pandas==2.0.3
